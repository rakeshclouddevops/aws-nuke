aws:
  region1: us-east-1
  region2: us-east-2
  access_key_id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  secret_access_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
cloudnuke:
  version: v0.37.1
  services:
    - ec2
    - s3
    - rds
